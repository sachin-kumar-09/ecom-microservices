server:
  port: 8888

spring:
  application:
    name: ConfigServer

  cloud:
    config:
      server:
        git:
          uri: file:///C:/Users/kumar/utility-files/config-repo

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.springframework.cloud.config: DEBUG