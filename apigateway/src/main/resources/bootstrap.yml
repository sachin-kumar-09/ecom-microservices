spring:
  application:
    name: ApiGateway   # Name of the service, used by Config Server
  config:
    import: "optional:configserver:http://localhost:8888"  # Pull config from local configserver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/   # Point to the discovery server
